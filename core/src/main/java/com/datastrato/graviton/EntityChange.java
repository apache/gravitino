package com.datastrato.graviton;

/** Mark interface for entity changes, all the entity changes must inherit this interface. */
public interface EntityChange {

  // TODO. Update the audit info of the entity. @Jerry
}
