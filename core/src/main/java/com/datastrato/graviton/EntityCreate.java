package com.datastrato.graviton;

/**
 * Mark interface for entity creation, all the entity creation must inherit this interface.
 */
public interface EntityCreate {
  // TODO. Update the audit info of the entity. @Jerry
}
